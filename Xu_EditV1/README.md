# Multi-Agent Education Paper

Working project for a paper draft on when multi-agent systems should replace or extend classical RAG as the orchestration pattern in educational settings.

## Current status

- Literature, benchmark, and evaluation-harness research has been consolidated into the draft and supporting notes.
- A LaTeX toolchain is not currently installed on this machine (`latexmk`, `pdflatex`, `bibtex`, and `biber` were not found).
- The manuscript is currently framed as a taxonomy + evaluation-protocol + benchmark-mapping paper, not as a completed empirical study.

## Planned structure

- `main.tex` — root manuscript file
- `sections/` — section-level `.tex` files
- `references/` — bibliography and source tracking
- `notes/` — working notes, outline, evaluation ideas, and open questions
- `assets/` — figures and diagrams

## Immediate next steps

1. Continue tightening prose and adding any missing citations for specific claims.
2. Decide whether to stay with `article` or migrate to an IEEE template.
3. Add any remaining BibTeX entries needed for benchmark or system references.
4. Compile and fix LaTeX issues once a TeX toolchain is available.
